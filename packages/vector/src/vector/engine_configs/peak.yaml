engine: peak
version: "1.0"
requirements:
  base_window: 8
  min_window: 4
  max_window: 256
  scaling: linear
outputs:
  - peak_value
  - peak_to_peak
dependencies: []
metadata:
  description: "Peak and peak-to-peak"
  category: basic
