"""
ML Entry Points
===============

CLI entry points for ML workflows.

Usage:
    python -m prime.ml.entry_points.cycle_features build --obs ... --manifold ... --output ...
    python -m prime.ml.entry_points.cycle_features train --train ... --test ... --rul ...
    python -m prime.ml.entry_points.train
    python -m prime.ml.entry_points.predict
    python -m prime.ml.entry_points.features
    python -m prime.ml.entry_points.ablation
    python -m prime.ml.entry_points.benchmark
    python -m prime.ml.entry_points.baseline
"""
