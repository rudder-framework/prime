# PRIME Requirements
# ===================
# Orchestrator for the Rudder Framework. Classifies signals, generates manifests,
# calls Manifold for computation, and analyzes results.

# -----------------------------------------------------------------------------
# RUST-BACKED PRIMITIVES (REQUIRED)
# -----------------------------------------------------------------------------
pmtvs                       # Rust-accelerated math functions (hurst, entropy, lyapunov, etc.)

# -----------------------------------------------------------------------------
# DATA READING & STORAGE
# -----------------------------------------------------------------------------
polars>=0.20.0              # Fast DataFrame operations
duckdb>=0.10.0              # Per-domain queryable databases
pyarrow>=14.0.0             # Parquet support
pandas>=2.0.0               # DataFrame support (some components)
openpyxl>=3.1.0             # Excel file support (.xlsx)

# -----------------------------------------------------------------------------
# NUMERICAL & SCIENTIFIC
# -----------------------------------------------------------------------------
numpy>=1.24.0               # Numerical arrays
scipy>=1.11.0               # FFT, statistical functions, spatial, integrate

# -----------------------------------------------------------------------------
# STATISTICS & TIME SERIES
# -----------------------------------------------------------------------------
statsmodels>=0.14.0         # ADF, KPSS, ARCH tests

# -----------------------------------------------------------------------------
# MACHINE LEARNING
# -----------------------------------------------------------------------------
scikit-learn>=1.4.0         # Regression, classification, cross-validation
xgboost>=2.0.0              # Gradient boosting (optional, for ML features)

# -----------------------------------------------------------------------------
# CONFIGURATION & VALIDATION
# -----------------------------------------------------------------------------
pydantic>=2.0.0             # Config validation (JSON schemas)
pyyaml>=6.0.0               # YAML config support

# -----------------------------------------------------------------------------
# API & SERVER
# -----------------------------------------------------------------------------
fastapi>=0.110.0            # API for serving config/data
uvicorn>=0.27.0             # ASGI server
httpx>=0.27.0               # Async HTTP client

# -----------------------------------------------------------------------------
# VISUALIZATION
# -----------------------------------------------------------------------------
matplotlib>=3.8.0           # Visualization for manifold renderer

# -----------------------------------------------------------------------------
# STREAMING (OPTIONAL)
# -----------------------------------------------------------------------------
requests>=2.31.0            # HTTP client for crypto/external data sources

# -----------------------------------------------------------------------------
# DEVELOPMENT
# -----------------------------------------------------------------------------
pytest>=8.0.0               # Testing framework

# -----------------------------------------------------------------------------
# LOCAL PACKAGES (editable installs)
# -----------------------------------------------------------------------------
# Install these with: pip install -e packages/<name>
#
# Core packages:
#   pip install -e packages/typology
#   pip install -e packages/vector
#   pip install -e packages/geometry
#
# Compute packages:
#   pip install -e packages/eigendecomp
#   pip install -e packages/dynamics
#   pip install -e packages/pairwise
#   pip install -e packages/divergence
#   pip install -e packages/velocity
#   pip install -e packages/thermodynamics
#   pip install -e packages/topology
#   pip install -e packages/stability
#   pip install -e packages/baseline
#   pip install -e packages/breaks
#   pip install -e packages/ridge
#   pip install -e packages/fleet
#
# Orchestration (depends on above packages):
#   pip install -e packages/orchestration
