# PRIME Requirements
# ===================
# Pure interpreter. Zero calculations. Reads parquet, applies config, serves UI.

# -----------------------------------------------------------------------------
# CORE DEPENDENCY
# -----------------------------------------------------------------------------
# orthon-engines @ git+https://github.com/orthon-engines/engines.git  # TODO: not yet a Python package
# Or after PyPI publish:
# prism-engine>=1.0.0

# -----------------------------------------------------------------------------
# DATA READING
# -----------------------------------------------------------------------------
polars>=0.20.0              # Read parquet files (fast, no pandas bloat)
duckdb>=0.10.0              # Per-domain queryable databases
pyarrow>=14.0.0             # Parquet support for DuckDB
openpyxl>=3.1.0             # Excel file support (.xlsx)

# -----------------------------------------------------------------------------
# TYPOLOGY (signal classification for manifest generation)
# -----------------------------------------------------------------------------
numpy>=1.24.0               # Numerical arrays
scipy>=1.11.0               # FFT, statistical functions
statsmodels>=0.14.0         # ADF, KPSS, ARCH tests

# -----------------------------------------------------------------------------
# MACHINE LEARNING / REGRESSION
# -----------------------------------------------------------------------------
scikit-learn>=1.4.0         # Regression, classification, cross-validation

# -----------------------------------------------------------------------------
# CONFIGURATION
# -----------------------------------------------------------------------------
pydantic>=2.0.0             # Config validation (JSON schemas)
tomli>=2.0.0                # TOML config support (Python <3.11)
pyyaml>=6.0.0               # YAML config support (optional)

# -----------------------------------------------------------------------------
# EXPLORER / UI
# -----------------------------------------------------------------------------
matplotlib>=3.8.0           # Visualization for manifold renderer
pandas>=2.0.0               # DataFrame support

# Option A: Built-in HTTP server (python -m orthon.explorer.server)
# No additional dependencies needed

# Option B: If you want a Python backend with FastAPI
fastapi>=0.110.0            # API for serving config/data
uvicorn>=0.27.0             # ASGI server

# -----------------------------------------------------------------------------
# REPORTING / NARRATIVE
# -----------------------------------------------------------------------------
jinja2>=3.1.0               # Template engine for reports
markdown>=3.5.0             # Markdown rendering for thesis output

# -----------------------------------------------------------------------------
# DEVELOPMENT
# -----------------------------------------------------------------------------
pytest>=8.0.0               # Testing framework
# ruff>=0.2.0               # Linting (optional)
# mypy>=1.8.0               # Type checking (optional)

# -----------------------------------------------------------------------------
# DATA SOURCING
# -----------------------------------------------------------------------------
kaggle>=1.6.0               # Kaggle API for dataset downloads
